<h1>Ruby Shop</h1>

<div class='search'>
  <%= form_with url: search_path, method: :get, local: true do |f| %>
    <%= f.text_field :q, id: :q, required: true, placeholder: "Znajdź produkt" %>
    <%= f.button'<i class="fas fa-search"></i>'.html_safe, class: 'search_loop' %>
  <% end %>
</div>

<%= render 'products' %>
